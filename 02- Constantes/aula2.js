let idade = 20; // usar o let permite que o valor possa ser reatribuido 
idade = 12;
console.log(idade);


// porém se for utilizado o 'const' o valor da variavel se torna constante, ou seja, não pode ser alterado sempre o mesmo valor

const numero = 6;
console.log(numero);
numero = 3; // isso gera um erro porque o const não pode mudar o valor ele é uma constante seu valor é 6.


// Dica usar o consta apenas para valores que não precisaram ser redefinidos